<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             Title="ROBINSONS MALLS">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <Button Text="ANTIPOLO                 →" 
                    HeightRequest="80"
                    FontSize="22"
                    BackgroundColor="White"
                    TextColor="Black"
                    BorderColor="#CCCCCC"
                    BorderWidth="1"
                    CornerRadius="8"
                    HorizontalOptions="Fill" />

            <Button Text="CAINTA                    →" 
                    HeightRequest="80"
                    FontSize="22"
                    BackgroundColor="White"
                    TextColor="Black"
                    BorderColor="#CCCCCC"
                    BorderWidth="1"
                    CornerRadius="8"
                    HorizontalOptions="Fill" />

            <Button Text="MAGNOLIA               →" 
                    HeightRequest="80"
                    FontSize="22"
                    BackgroundColor="White"
                    TextColor="Black"
                    BorderColor="#CCCCCC"
                    BorderWidth="1"
                    CornerRadius="8"
                    HorizontalOptions="Fill" />

            <Button Text="GALLERIA                →" 
                    HeightRequest="80"
                    FontSize="22"
                    BackgroundColor="White"
                    TextColor="Black"
                    BorderColor="#CCCCCC"
                    BorderWidth="1"
                    CornerRadius="8"
                    HorizontalOptions="Fill" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>